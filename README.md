# 📝 Task Manager CLI

A simple command-line application to manage daily tasks.  
Users can add new tasks, view the task list, and mark tasks as completed.  
All tasks are stored in a JSON file for persistence.

---

## 🚀 Features
- Add new tasks  
- Display task list with status (done / not done)  
- Mark tasks as completed  
- Save tasks in `tasks.json`  

---

## 📂 Project Structure
